# GitHub Secrets Template
# This file lists all required secrets for the GitHub Actions CI/CD workflow
# DO NOT commit actual secret values to the repository!

# ============================================================================
# DockerHub Credentials
# ============================================================================
# Your DockerHub username
# Example: myusername
DOCKERHUB_USERNAME=

# DockerHub access token (NOT your password!)
# Create at: https://hub.docker.com/settings/security
# Permissions needed: Read, Write, Delete
DOCKERHUB_TOKEN=

# ============================================================================
# SSH Deployment Credentials
# ============================================================================
# Private SSH key for accessing the production server
# Generate with: ssh-keygen -t ed25519 -C "github-actions@forex-trading"
# Format: Copy the entire content of the private key file including:
#   -----BEGIN OPENSSH PRIVATE KEY-----
#   [key content]
#   -----END OPENSSH PRIVATE KEY-----
SSH_PRIVATE_KEY=

# Production server hostname or IP address
# Examples: 
#   - trading.example.com
#   - 192.168.1.100
#   - my-server.ddns.net
SERVER_HOST=

# SSH username for the production server
# Examples: ubuntu, deploy, admin
SERVER_USER=

# Absolute path on the server where the application is deployed
# Example: /home/ubuntu/forex-trading-system
# Note: This directory must exist and be writable by SERVER_USER
DEPLOY_PATH=

# ============================================================================
# How to Set These Secrets in GitHub
# ============================================================================
# 1. Go to your GitHub repository
# 2. Click on "Settings" tab
# 3. In the left sidebar, click "Secrets and variables" → "Actions"
# 4. Click "New repository secret"
# 5. Enter the secret name (e.g., DOCKERHUB_USERNAME)
# 6. Enter the secret value
# 7. Click "Add secret"
# 8. Repeat for all secrets listed above

# ============================================================================
# Verification Checklist
# ============================================================================
# Before running the workflow, ensure:
# [ ] All 6 secrets are configured in GitHub
# [ ] DockerHub token has write permissions
# [ ] SSH key is added to server's authorized_keys
# [ ] Server user can run Docker commands (in docker group)
# [ ] Deployment directory exists on server
# [ ] Server is accessible from GitHub Actions (no firewall blocking)
# [ ] docker-compose.yaml is valid (run: docker compose config)

# ============================================================================
# Testing Your Setup
# ============================================================================
# Test SSH connection:
#   ssh -i /path/to/private-key SERVER_USER@SERVER_HOST
#
# Test Docker access on server:
#   ssh SERVER_USER@SERVER_HOST "docker ps"
#
# Test deployment directory:
#   ssh SERVER_USER@SERVER_HOST "ls -la DEPLOY_PATH"
#
# Validate workflow locally:
#   ./.github/scripts/validate-workflow.sh
